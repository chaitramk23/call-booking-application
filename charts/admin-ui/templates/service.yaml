apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin-ui.fullname" . }}
  labels:
    app: {{ include "admin-ui.name" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: {{ include "admin-ui.name" . }}
