apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-ui.fullname" . }}
  labels:
    app: {{ include "user-ui.name" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: {{ include "user-ui.name" . }}
