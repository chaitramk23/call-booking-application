apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin-api.fullname" . }}
  labels:
    app: {{ include "admin-api.name" . }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: {{ include "admin-api.name" . }}
